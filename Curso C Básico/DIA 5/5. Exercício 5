#include <stdio.h>
#include<locale.h>


void main(){
    setlocale(LC_ALL, "Portuguese");
    int soma, numero;

    for (int i = 0; i< 10; i++){
            scanf("%d", &numero);
            soma+=numero;
        }
        printf("%d", soma/10);
    }
